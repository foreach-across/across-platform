<?xml version="1.0" encoding="UTF-8"?>
<config xmlns="http://www.ehcache.org/v3">

    <cache alias="securityPrincipalCache">
        <resources>
            <heap unit="entries">10</heap>
        </resources>
    </cache>
    <cache alias="userCache">
        <resources>
            <heap unit="entries">10</heap>
        </resources>
    </cache>
    <cache alias="userPropertiesCache">
        <resources>
            <heap unit="entries">10</heap>
        </resources>
    </cache>
    <cache alias="groupCache">
        <resources>
            <heap unit="entries">10</heap>
        </resources>
    </cache>
    <cache alias="groupPropertiesCache">
        <resources>
            <heap unit="entries">10</heap>
        </resources>
    </cache>

</config>
